# HedgeModManager Launcher
Proof of concept for a "big picture" style launcher for HedgeModManager

## How to Build and Test
`npm run build` - Build executable

`npm run serve` - Serve to `localhost:9000` with live reload